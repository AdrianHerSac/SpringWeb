<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>{{ titulo }}</title>
    <!-- Bootstrap CSS + Fuente Medieval -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=MedievalSharp&family=Cinzel:wght@700&display=swap" rel="stylesheet">

    <style>
        body {
            background-color: #2b1d0e;
            color: #f4e4bc;
            font-family: 'MedievalSharp', cursive;
            padding-top: 50px;
        }
        .card {
            background-color: #3e2723;
            border: 2px solid #d4af37;
        }
        label {
            color: #d4af37;
            font-family: 'Cinzel', serif;
        }
        .form-control, .form-select {
            background-color: #5d4037;
            border: 1px solid #8d6e63;
            color: #f4e4bc;
        }
        .form-control:focus, .form-select:focus {
            background-color: #4e342e;
            color: #fff;
            box-shadow: 0 0 10px #d4af37;
            border-color: #d4af37;
        }
        /* Placeholder styling for darker inputs */
        .form-control::placeholder {
            color: #a1887f;
            opacity: 0.8;
        }
        .btn-gold {
            background-color: #d4af37;
            color: #2b1d0e;
            font-weight: bold;
            font-family: 'Cinzel', serif;
            border: none;
        }
        .btn-gold:hover {
            background-color: #ffc107;
        }
    </style>
</head>
<body>

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card p-4 shadow-lg">
                <h2 class="text-center mb-4" style="color: #d4af37; font-family: 'Cinzel', serif;">
                    {{ titulo }}
                </h2>

                <form action="/productos/guardar" method="post">

                    <input type="hidden" name="id" value="{{ producto.id }}">

                    <!-- NOMBRE -->
                    <div class="mb-3">
                        <label for="nombre" class="form-label">Nombre del Peluche:</label>
                        <input type="text" class="form-control" id="nombre" name="nombre" value="{{ producto.nombre }}" required>
                    </div>

                    <!-- PRECIO -->
                    <div class="mb-3">
                        <label for="precio" class="form-label">Precio (Oro):</label>
                        <input type="number" class="form-control" id="precio" name="precio" value="{{ producto.precio }}" step="0.01" required>
                    </div>

                    <!-- NUEVA FILA: STOCK Y COLOR -->
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label for="stock" class="form-label">Stock en Almacén:</label>
                            <input type="number" class="form-control" id="stock" name="stock" value="{{ producto.stock }}" required>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label for="color" class="form-label">Color del Pelaje:</label>
                            <input type="text" class="form-control" id="color" name="color" value="{{ producto.color }}" placeholder="Ej: Rojo Fuego">
                        </div>
                    </div>

                    <!-- NUEVO: SELECTOR DE TALLA -->
                    <div class="mb-3">
                        <label for="size" class="form-label">Tamaño:</label>
                        <select class="form-select" id="size" name="size" required>
                            <option value="" disabled selected>-- Elige tamaño --</option>
                            {% for talla in tallas %}
                                <option value="{{ talla }}"
                                    {% if producto.size == talla %} selected {% endif %}>
                                    {{ talla }}
                                </option>
                            {% endfor %}
                        </select>
                    </div>

                    <!-- CATEGORÍA -->
                    <div class="mb-3">
                        <label for="categoria" class="form-label">Clase de Dragón:</label>
                        <select class="form-select" id="categoria" name="categoria" required>
                            <option value="" disabled selected>-- Selecciona una clase --</option>
                            {% for categoria in categorias %}
                                <option value="{{ categoria.id }}"
                                    {% if producto.categoria != null and producto.categoria.id == categoria.id %} selected {% endif %}>
                                    {{ categoria.nombre }}
                                </option>
                            {% endfor %}
                        </select>
                    </div>

                    <!-- HISTORIA -->
                    <div class="mb-3">
                        <label for="historia" class="form-label">Descripción / Detalles:</label>
                        <textarea class="form-control" id="historia" name="historia" rows="3">{{ producto.historia }}</textarea>
                    </div>

                    <!-- IMAGEN -->
                    <div class="mb-4">
                        <label for="imagen" class="form-label">Retrato (URL):</label>
                        <input type="text" class="form-control" id="imagen" name="imagen" value="{{ producto.imagen }}" placeholder="https://...">
                    </div>

                    <!-- BOTONES -->
                    <div class="d-grid gap-2">
                        <button type="submit" class="btn btn-gold btn-lg">Guardar en Inventario</button>
                        <a href="/productos" class="btn btn-outline-light">Cancelar</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

</body>
</html>