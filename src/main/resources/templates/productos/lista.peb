<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Dracopedia - El Libro de los Dragones</title>

    <link href="https://fonts.googleapis.com/css2?family=MedievalSharp&family=Cinzel:wght@700&display=swap" rel="stylesheet">

    <style>
        body {
            background-color: #2b1d0e; /* Marrón muy oscuro */
            background-image: repeating-linear-gradient(45deg, #2b1d0e 0, #2b1d0e 1px, #1f140a 0, #1f140a 50%);
            font-family: 'MedievalSharp', cursive;
            margin: 0;
            padding: 40px 20px;
            color: #3e2723;
            min-height: 100vh;
            display: flex;
            justify-content: center;
        }

        .parchment {
            background-color: #f4e4bc;
            background-image:
                linear-gradient(to right, rgba(0,0,0,0.1) 0%, rgba(0,0,0,0) 5%, rgba(0,0,0,0) 95%, rgba(0,0,0,0.1) 100%),
                url("data:image/svg+xml,%3Csvg width='100' height='100' viewBox='0 0 100 100' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.5' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='0.05'/%3E%3C/svg%3E");

            width: 100%;
            max-width: 800px;
            padding: 60px 50px;
            box-shadow:
                0 0 30px rgba(0,0,0,0.8), /* Sombra profunda */
                inset 0 0 60px rgba(139, 69, 19, 0.3); /* Bordes envejecidos */
            border-radius: 5px 15px 15px 5px; /* Borde suave */
            position: relative;
            border-left: 10px solid #3e2723; /* Simula el lomo del libro */
        }

        h1 {
            font-family: 'Cinzel', serif;
            text-align: center;
            font-size: 3rem;
            color: #5d4037;
            text-shadow: 1px 1px 0px rgba(255,255,255,0.5);
            border-bottom: 3px double #5d4037;
            padding-bottom: 20px;
            margin-bottom: 40px;
            text-transform: uppercase;
        }

        .total-count {
            font-size: 1rem;
            color: #8d6e63;
            display: block;
            margin-top: 5px;
            font-family: 'MedievalSharp', cursive;
            text-transform: none;
        }

        ul {
            list-style-type: none;
            padding: 0;
        }

        .dragon-entry {
            display: flex;
            align-items: center;
            border-bottom: 1px solid rgba(93, 64, 55, 0.3);
            padding: 20px 0;
            transition: background-color 0.3s;
        }

        .dragon-entry:hover {
            background-color: rgba(93, 64, 55, 0.05); /* Efecto sutil al pasar el ratón */
        }

        .dragon-entry:last-child {
            border-bottom: none;
        }

        .dragon-img {
            width: 80px;
            height: 80px;
            object-fit: cover;
            border: 3px double #5d4037;
            border-radius: 50%; /* Redonda como un medallón */
            margin-right: 25px;
            background-color: #ddd;
            box-shadow: 2px 2px 5px rgba(0,0,0,0.3);
            filter: sepia(30%); /* Un toque antiguo */
        }

        .dragon-info {
            flex-grow: 1;
        }

        .dragon-name {
            font-size: 1.6rem;
            font-weight: bold;
            color: #2c1e16;
            margin: 0;
        }

        .dragon-price {
            font-size: 1.2rem;
            color: #8f260a; /* Rojo oscuro para el precio */
        }

        .btn-ink {
            text-decoration: none;
            color: #3e2723;
            font-weight: bold;
            border: 2px solid #3e2723;
            padding: 8px 15px;
            border-radius: 30px 5px 30px 5px; /* Forma irregular */
            transition: all 0.3s;
            font-size: 0.9rem;
            white-space: nowrap;
        }

        .btn-ink:hover {
            background-color: #3e2723;
            color: #f4e4bc;
            transform: scale(1.05);
        }

        .empty-message {
            text-align: center;
            font-size: 1.5rem;
            color: #8d6e63;
            font-style: italic;
            padding: 40px;
        }
    </style>
</head>
<body>

    <div class="parchment">
        <h1>
            Dracopedia
            <span class="total-count">~ Avistamientos registrados: {{ total }} ~</span>
        </h1>

        <ul>
            {% for producto in productos %}
                <li class="dragon-entry">
                    <img src="{{ producto.imagen is empty ? 'https://placehold.co/100/5d4037/f4e4bc?text=?' : producto.imagen }}"
                         alt="{{ producto.nombre }}"
                         class="dragon-img">

                    <div class="dragon-info">
                        <div class="dragon-name">{{ producto.nombre }}</div>
                        <div class="dragon-price">{{ producto.precio }} Monedas de Oro</div>
                    </div>

                    <a href="/productos/{{ producto.id }}" class="btn-ink">
                        Investigar ➜
                    </a>
                </li>
            {% else %}
                <li class="empty-message">
                    "Las páginas están en blanco... Aún no se han avistado dragones en estas tierras."
                </li>
            {% endfor %}
        </ul>

        <div style="text-align: center; margin-top: 40px; color: #5d4037; font-size: 20px;">
            ❦
        </div>
    </div>

</body>
</html>