<!DOCTYPE html>
<html>
<head>
    <title>Lista de Productos</title>
</head>
<body>
    <h1>Lista de productos (Total: {{ total }})</h1>

    <ul>
        {% for producto in productos %}
            <li>
                <strong>{{ producto.nombre }}</strong> - {{ producto.precio }}  <a href="/productos/{{ producto.id }}">Ver detalle</a>
            </li>
        {% else %}
            <li>No hay productos encontrados.</li>
        {% endfor %}
    </ul>
</body>
</html>