<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <title>Dracopedia - El Libro de los Dragones</title>

    <!-- IMPORTANTE: Faltaban estas líneas para que funcionen los botones y los iconos -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">

    <link rel="stylesheet" href="/css/estilos.css">

    <!-- Fuentes -->
    <link href="https://fonts.googleapis.com/css2?family=MedievalSharp&family=Cinzel:wght@700&display=swap" rel="stylesheet">

    <style>
        body {
            background-color: #2b1d0e;
            background-image: repeating-linear-gradient(45deg, #2b1d0e 0, #2b1d0e 1px, #1f140a 0, #1f140a 50%);
            font-family: 'MedievalSharp', cursive;
            margin: 0;
            padding: 40px 20px;
            color: #3e2723;
            min-height: 100vh;
            display: flex;
            justify-content: center;
        }

        .parchment {
            background-color: #f4e4bc;
            background-image:
                linear-gradient(to right, rgba(0, 0, 0, 0.1) 0%, rgba(0, 0, 0, 0) 5%, rgba(0, 0, 0, 0) 95%, rgba(0, 0, 0, 0.1) 100%),
                url("data:image/svg+xml,%3Csvg width='100' height='100' viewBox='0 0 100 100' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.5' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='0.05'/%3E%3C/svg%3E");

            width: 100%;
            max-width: 900px;
            padding: 50px 40px;
            box-shadow:
                0 0 30px rgba(0, 0, 0, 0.8),
                inset 0 0 60px rgba(139, 69, 19, 0.3);
            border-radius: 15px 25px 15px 5px;
            position: relative;
            border-left: 10px solid #3e2723;
        }

        h1 {
            font-family: 'Cinzel', serif;
            text-align: center;
            font-size: 3rem;
            color: #5d4037;
            text-shadow: 1px 1px 0px rgba(255, 255, 255, 0.5);
            border-bottom: 3px double #5d4037;
            padding-bottom: 20px;
            margin-bottom: 30px;
            text-transform: uppercase;
        }

        .total-count {
            font-size: 1rem;
            color: #8d6e63;
            display: block;
            margin-top: 5px;
            font-family: 'MedievalSharp', cursive;
            text-transform: none;
        }

        .search-container {
            display: flex;
            justify-content: center;
            margin-bottom: 40px;
            position: relative;
        }

        .search-form {
            position: relative;
            width: 100%;
            max-width: 500px;
            display: flex;
            align-items: center;
            border-bottom: 2px solid #5d4037;
            padding-bottom: 5px;
            transition: border-color 0.3s;
        }

        .search-form:focus-within {
            border-bottom-color: #d4af37;
        }

        .search-input {
            width: 100%;
            border: none;
            background: transparent;
            font-family: 'MedievalSharp', cursive;
            font-size: 1.2rem;
            color: #2c1e16;
            outline: none;
            padding: 5px;
        }

        .search-input::placeholder {
            color: #8d6e63;
            opacity: 0.7;
            font-style: italic;
        }

        .search-btn {
            background-color: #5d4037;
            color: #f4e4bc;
            border: none;
            padding: 8px 20px;
            font-family: 'Cinzel', serif;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 1px;
            border-radius: 5px 20px 5px 20px;
            cursor: pointer;
            transition: all 0.3s;
            margin-left: 10px;
        }

        .search-btn:hover {
            background-color: #3e2723;
            color: #d4af37;
            transform: scale(1.05);
            box-shadow: 2px 2px 5px rgba(0,0,0,0.2);
        }

        .clear-link {
            text-decoration: none;
            color: #b71c1c;
            font-size: 0.9rem;
            margin-left: 10px;
            display: {{ busqueda is empty ? 'none' : 'block' }};
        }

        ul {
            list-style-type: none;
            padding: 0;
        }

        .dragon-entry {
            display: flex;
            align-items: center;
            border-bottom: 1px solid rgba(93, 64, 55, 0.3);
            padding: 20px 0;
            transition: background-color 0.3s;
        }

        .dragon-entry:hover {
            background-color: rgba(93, 64, 55, 0.05);
        }

        .dragon-entry:last-child {
            border-bottom: none;
        }

        .dragon-img {
            width: 90px;
            height: 90px;
            object-fit: cover;
            border: 3px double #5d4037;
            border-radius: 50%;
            margin-right: 25px;
            background-color: #ddd;
            box-shadow: 2px 2px 5px rgba(0, 0, 0, 0.3);
            filter: sepia(30%);
        }

        .dragon-info {
            flex-grow: 1;
        }

        .dragon-name {
            font-size: 1.6rem;
            font-weight: bold;
            color: #2c1e16;
            margin: 0;
        }

        .dragon-price {
            font-size: 1.2rem;
            color: #8f260a;
        }

        .btn-ink {
            text-decoration: none;
            color: #3e2723;
            font-weight: bold;
            border: 2px solid #3e2723;
            padding: 8px 15px;
            border-radius: 30px 5px 30px 5px;
            transition: all 0.3s;
            font-size: 0.9rem;
            white-space: nowrap;
        }

        .btn-ink:hover {
            background-color: #3e2723;
            color: #f4e4bc;
            transform: scale(1.05);
        }

        .empty-message {
            text-align: center;
            font-size: 1.5rem;
            color: #8d6e63;
            font-style: italic;
            padding: 40px;
        }

        .actions-bar {
            margin-top: 40px;
            border-top: 1px dashed #8d6e63;
            padding-top: 20px;
            display: flex;
            justify-content: flex-start;
            align-items: center;
        }

        .btn-medieval {
            font-family: 'Cinzel', serif;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
    </style>
</head>

<body>
{% include "fragments/nav" %}

    <div class="parchment">
        <h1>
            Dracopedia
            <span class="total-count">~ Avistamientos registrados: {{ total }} ~</span>
        </h1>

        <div class="search-container">
            <form action="/productos" method="GET" class="search-form">
                <input type="text"
                       name="busqueda"
                       value="{{ busqueda }}"
                       class="search-input"
                       placeholder="Rastrear bestia por nombre..."
                >
                <button type="submit" class="search-btn">Rastrear</button>

                {% if busqueda != null %}
                    <a href="/productos" class="clear-link" title="Borrar filtros">✖</a>
                {% endif %}
            </form>
        </div>

        <ul>
            {% for producto in productos %}
            <li class="dragon-entry">
                <img src="{{ producto.imagen is empty ? 'https://placehold.co/100/5d4037/f4e4bc?text=?' : producto.imagen }}"
                    alt="{{ producto.nombre }}" class="dragon-img">

                <div class="dragon-info">
                    <div class="dragon-name">
                        {{ producto.nombre }}
                        {% if producto.stock == 0 %}
                            <span class="badge bg-danger" style="font-family: sans-serif; font-size: 0.6rem; vertical-align: middle;">AGOTADO</span>
                        {% endif %}
                    </div>
                    <div class="dragon-price">{{ producto.precio }} Monedas de Oro</div>
                </div>

                <a href="/productos/{{ producto.id }}" class="btn-ink">
                    Investigar ➜
                </a>
            </li>
            {% else %}
            <li class="empty-message">
                "No se han encontrado registros que coincidan con '{{ busqueda }}'..."
            </li>
            {% endfor %}
        </ul>

        <!-- Botón de Volver con estilo de Detalle -->
        <div class="actions-bar">
            <a href="/" class="btn btn-outline-dark btn-medieval">
                <i class="bi bi-arrow-left"></i> Volver a la Aldea
            </a>
        </div>

        <div style="text-align: center; margin-top: 40px; color: #5d4037; font-size: 20px;">
            ❦
        </div>
    </div>
</body>
</html>