<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Formalizar Trato - {{ producto.nombre }}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=MedievalSharp&family=Cinzel:wght@700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">

    <style>
        body {
            background-color: #2b1d0e;
            background-image: repeating-linear-gradient(45deg, #2b1d0e 0, #2b1d0e 1px, #1f140a 0, #1f140a 50%);
            font-family: 'MedievalSharp', cursive;
            padding: 40px 20px;
            color: #3e2723;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .contract-paper {
            background-color: #fdfbf7;
            background-image: url("https://www.transparenttextures.com/patterns/cream-paper.png");
            padding: 50px;
            max-width: 700px;
            width: 100%;
            border: 10px solid #5d4037; /* Marco grueso tipo cuadro */
            box-shadow: 0 0 50px rgba(0,0,0,0.9);
            position: relative;
        }

        .seal {
            position: absolute;
            top: -20px;
            right: -20px;
            font-size: 80px;
            color: #b71c1c;
            text-shadow: 2px 2px 5px rgba(0,0,0,0.5);
            transform: rotate(15deg);
        }

        h1 {
            font-family: 'Cinzel', serif;
            text-align: center;
            color: #2c1e16;
            border-bottom: 2px solid #2c1e16;
            padding-bottom: 20px;
            margin-bottom: 30px;
            text-transform: uppercase;
        }

        .product-summary {
            background-color: rgba(93, 64, 55, 0.1);
            padding: 20px;
            border-radius: 5px;
            margin-bottom: 30px;
            display: flex;
            align-items: center;
            gap: 20px;
        }

        .mini-img {
            width: 80px;
            height: 80px;
            object-fit: cover;
            border: 2px solid #5d4037;
            border-radius: 50%;
        }

        .total-box {
            font-size: 1.5rem;
            text-align: right;
            margin-top: 20px;
            padding-top: 20px;
            border-top: 2px dashed #5d4037;
            color: #b71c1c;
            font-weight: bold;
        }

        .btn-sign {
            width: 100%;
            background-color: #2c1e16;
            color: #d4af37;
            font-family: 'Cinzel', serif;
            font-size: 1.2rem;
            padding: 15px;
            border: 2px solid #d4af37;
            margin-top: 20px;
            transition: all 0.3s;
        }

        .btn-sign:hover {
            background-color: #d4af37;
            color: #2c1e16;
        }
    </style>
</head>
<body>

<div class="contract-paper">
    <i class="bi bi-patch-check-fill seal"></i>

    <h1>Contrato de Adquisición</h1>

    <p class="text-center mb-4">
        Por la presente, el comprador se compromete a adquirir la mercancía descrita a continuación,
        pagando su valor en oro y prometiendo cuidar de la bestia (o peluche).
    </p>

    <div class="product-summary">
        <img src="{{ producto.imagen is empty ? 'https://placehold.co/100?text=?' : producto.imagen }}" class="mini-img">
        <div>
            <h3 style="margin: 0; font-family: 'Cinzel', serif;">{{ producto.nombre }}</h3>
            <div class="text-muted">{{ producto.color }} - Talla {{ producto.size }}</div>
            <div class="text-success fw-bold">Stock disponible: {{ producto.stock }}</div>
        </div>
    </div>

    <form action="/productos/compra" method="post">

        <input type="hidden" name="id" value="{{ producto.id }}">

        <input type="hidden" id="precioUnitario" value="{{ producto.precio }}">

        <div class="row align-items-center">
            <div class="col-md-6">
                <label for="cantidad" class="form-label fw-bold">Cantidad:</label>
                <input type="number"
                       id="cantidad"
                       name="cantidad"
                       class="form-control"
                       value="1"
                       min="1"
                       max="{{ producto.stock }}"
                       required
                       style="background: #fff; border: 2px solid #5d4037;">
            </div>
            <div class="col-md-6 text-end">
                <div class="text-muted">Precio Unitario</div>
                <div class="fs-5">{{ producto.precio }} Oro</div>
            </div>
        </div>

        <div class="total-box">
            Total a Pagar: <span id="precioTotal">{{ producto.precio }}</span> Oro
        </div>

        <button type="submit" class="btn btn-sign">
            <i class="bi bi-feather"></i> Firmar y Pagar
        </button>

        <div class="text-center mt-3">
            <a href="/productos/{{ producto.id }}" class="text-muted" style="text-decoration: none;">
                Romper contrato (Cancelar)
            </a>
        </div>
    </form>
</div>

<script>
    const inputCantidad = document.getElementById('cantidad');
    const spanTotal = document.getElementById('precioTotal');
    const precioUnitario = parseFloat(document.getElementById('precioUnitario').value);

    inputCantidad.addEventListener('input', function() {
        let cantidad = parseInt(this.value) || 0;

        if(cantidad < 1) cantidad = 1;

        let total = (cantidad * precioUnitario).toFixed(2);
        spanTotal.textContent = total;
    });
</script>

</body>
</html>