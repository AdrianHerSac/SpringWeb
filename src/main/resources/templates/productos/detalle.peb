<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>{{ producto.nombre }} - Detalles</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=MedievalSharp&family=Cinzel:wght@700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">

    <style>
        body {
            background-color: #2b1d0e;
            background-image: repeating-linear-gradient(45deg, #2b1d0e 0, #2b1d0e 1px, #1f140a 0, #1f140a 50%);
            font-family: 'MedievalSharp', cursive;
            padding: 40px 20px;
            color: #3e2723;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .parchment-page {
            background-color: #f4e4bc;
            background-image: url("https://www.transparenttextures.com/patterns/aged-paper.png");
            box-shadow: 0 0 50px rgba(0,0,0,0.8);
            border-radius: 5px;
            padding: 50px;
            max-width: 900px;
            width: 100%;
            position: relative;
            border: 1px solid #d7c496;
        }

        h1 {
            font-family: 'Cinzel', serif;
            color: #2c1e16;
            font-size: 3rem;
            border-bottom: 3px double #5d4037;
            padding-bottom: 10px;
            margin-bottom: 30px;
        }

        .dragon-portrait-container {
            text-align: center;
            padding: 15px;
            background: #fff;
            border: 1px solid #ccc;
            box-shadow: 3px 3px 10px rgba(0,0,0,0.3);
            transform: rotate(-2deg); /* Un poco torcida para realismo */
            margin-bottom: 20px;
        }

        .dragon-img {
            width: 100%;
            height: auto;
            max-height: 400px;
            object-fit: cover;
            filter: sepia(20%) contrast(110%);
        }

        .info-label {
            color: #8d6e63;
            font-size: 0.9rem;
            text-transform: uppercase;
            font-family: 'Cinzel', serif;
            margin-bottom: 5px;
            display: block;
        }

        .info-value {
            font-size: 1.4rem;
            margin-bottom: 20px;
            border-left: 4px solid #d4af37;
            padding-left: 15px;
        }

        .price-tag {
            color: #b71c1c; /* Rojo sangre */
            font-size: 2rem;
            font-weight: bold;
        }

        .badge-category {
            background-color: #5d4037;
            color: #d4af37;
            padding: 5px 15px;
            border-radius: 20px;
            font-size: 1rem;
            font-family: 'Cinzel', serif;
        }

        .actions-bar {
            margin-top: 40px;
            border-top: 1px dashed #8d6e63;
            padding-top: 20px;
            display: flex;
            justify-content: space-between;
        }

        .btn-medieval {
            font-family: 'Cinzel', serif;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
    </style>
</head>
<body>

<div class="parchment-page">

    <div class="row">
        <div class="col-md-5">
            <div class="dragon-portrait-container">
                <img src="{{ producto.imagen is empty ? 'https://placehold.co/400x500/5d4037/f4e4bc?text=Sin+Retrato' : producto.imagen }}"
                     alt="{{ producto.nombre }}"
                     class="dragon-img">
                <p class="mt-2 text-muted fst-italic">Fig. 1: Avistamiento reciente</p>
            </div>
        </div>

        <div class="col-md-7">
            <h1>{{ producto.nombre }}</h1>

            <div class="mb-4">
                <span class="info-label">Clase / Raza:</span>
                <span class="badge-category">
                    <i class="bi bi-fire"></i> {{ producto.categoria.nombre }}
                </span>
            </div>

            <div class="mb-4">
                <span class="info-label">Descripción del Domador:</span>
                <div class="info-value">
                    {{ producto.historia is empty ? "No hay registros sobre el comportamiento de esta bestia." : producto.historia }}
                </div>
            </div>

            <div class="mb-4">
                <span class="info-label">Valor de Mercado:</span>
                <div class="price-tag">
                    {{ producto.precio }} <small style="font-size: 1rem; color: #5d4037;">Monedas de Oro</small>
                </div>
            </div>

            <div class="actions-bar">
                <a href="/productos/lista" class="btn btn-outline-dark btn-medieval">
                    <i class="bi bi-arrow-left"></i> Volver al Bestiario
                </a>

                <div>
                    <a href="/productos/editar/{{ producto.id }}" class="btn btn-warning btn-medieval">
                        <i class="bi bi-pencil-fill"></i> Editar
                    </a>

                    <a href="/productos/borrar/{{ producto.id }}"
                       class="btn btn-danger btn-medieval ms-2"
                       onclick="return confirm('¿Seguro que quieres borrar esta página del libro?');">
                        <i class="bi bi-trash-fill"></i>
                    </a>
                </div>
            </div>
        </div>
    </div>

</div>

</body>
</html>