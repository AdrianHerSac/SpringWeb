<!DOCTYPE html>
<html lang="es">
<head>
    <title>Puertas de la Aldea</title>
    {% include "fragments/head" %}

    <title>{{ producto.nombre }} - Detalles</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=MedievalSharp&family=Cinzel:wght@700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/estilos.css">
    <link rel="stylesheet" href="/css/footer.css">
</head>
<body>

{% include "fragments/nav" %}

<div class="parchment-page">

    <div class="row">
        <!-- COLUMNA IZQUIERDA: FOTO -->
        <div class="col-md-5">
            <div class="dragon-portrait-container">
                <img src="{{ producto.imagen is empty ? 'https://placehold.co/400x500/5d4037/f4e4bc?text=Sin+Retrato' : producto.imagen }}"
                     alt="{{ producto.nombre }}" class="dragon-img">
                <p class="mt-2 text-muted fst-italic">Fig. 1: Muestra del producto</p>
            </div>
        </div>

        <!-- COLUMNA DERECHA: DATOS -->
        <div class="col-md-7">
            <h4>{{ producto.nombre }}</h1>

            <!-- CONTADOR DE VISITAS -->
            <div class="mb-3 text-muted" style="font-family: 'Cinzel', serif; font-size: 0.9rem;">
                <i class="bi bi-eye-fill"></i> {{ producto.visitas }} curiosos han observado este tesoro
            </div>

            <div class="mb-4">
                <span class="info-label">Clase / Raza:</span>
                <span class="badge-category">
                    <i class="bi bi-fire"></i> {{ producto.categoria.nombre }}
                </span>
            </div>

            <!-- TARJETA DE ESTADÍSTICAS (TALLA, COLOR, STOCK) -->
            <div class="stats-card">
                <div class="row text-center">
                    <div class="col-4 border-end border-secondary">
                        <small class="text-white-50 d-block" style="font-family: sans-serif; font-size: 0.7rem;">TALLA</small>
                        <span class="fs-4 text-warning fw-bold">{{ producto.size }}</span>
                    </div>
                    <div class="col-4 border-end border-secondary">
                        <small class="text-white-50 d-block" style="font-family: sans-serif; font-size: 0.7rem;">COLOR</small>
                        <span class="fs-6">{{ producto.color }}</span>
                    </div>
                    <div class="col-4">
                        <small class="text-white-50 d-block" style="font-family: sans-serif; font-size: 0.7rem;">STOCK</small>
                        {% if producto.stock > 0 %}
                            <span class="text-success fw-bold">{{ producto.stock }}</span>
                        {% else %}
                            <span class="text-danger fw-bold">AGOTADO</span>
                        {% endif %}
                    </div>
                </div>
            </div>

            <div class="mb-4">
                <span class="info-label">Descripción del Mercader:</span>
                <div class="info-value">
                    {{ producto.historia is empty ? "No hay detalles sobre la procedencia de este artículo." : producto.historia }}
                </div>
            </div>

            <div class="mb-4">
                <span class="info-label">Valor de Mercado:</span>
                <div class="price-tag">
                    {{ producto.precio }} <small style="font-size: 1rem; color: #5d4037;">Monedas de Oro</small>
                </div>
            </div>

            <!-- BARRA DE ACCIONES -->
            <div class="actions-bar">
                <a href="/productos" class="btn btn-outline-dark btn-medieval">
                    <i class="bi bi-arrow-left"></i> Volver
                </a>

                <div class="d-flex gap-2">
                    {% if request.userPrincipal != null %}
                        <!-- BOTONES PARA EL USUARIO -->
                        {% if producto.stock > 0 %}
                        <a href="/productos/compra/ {{ producto.id }}" class="btn btn-primary btn-medieval">
                        <i class="bi bi-basket-fill"></i> Comprar
                    </a>
                    {% else %}
                    <button class="btn btn-secondary btn-medieval" disabled> Sin Stock </button>
                    {% endif %}

                    <!-- BOTONES DE GESTIÓN (SOLO SI ESTÁ LOGUEADO) -->

                        <a href="/productos/editar/{{ producto.id }}" class="btn btn-warning btn-medieval" title="Editar">
                            <i class="bi bi-pencil-fill"></i>
                        </a>
                        <a href="/productos/borrar/{{ producto.id }}"
                           class="btn btn-danger btn-medieval" title="Borrar"
                           onclick="return confirm('¿Seguro que quieres borrar este artículo del inventario?');">
                            <i class="bi bi-trash-fill"></i>
                        </a>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
</div>
{% include "fragments/footer" %}
</body>
</html>